# Copy to .env.server and replace every blank secret before deployment.
# Generate two independent random values containing at least 32 characters.
SECRET_KEY=
JWT_SECRET_KEY=
PUBLIC_ORIGIN=http://localhost:8080
JWT_COOKIE_SECURE=false
SERVER_PORT=8080
SECURITY_HSTS_SECONDS=31536000
